'use strict';

document.addEventListener('DOMContentLoaded', init);

function init() {
    // event bindings
    document.querySelector('#video-input').addEventListener('click', displayVideoInput);
}

function displayVideoInput(e) {
    e.preventDefault();

    
}
